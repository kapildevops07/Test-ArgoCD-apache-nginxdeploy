apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Values.namespace }}
  name: apache-svc
  labels:
    app: apache-svc
spec:
  #type: LoadBalancer
  ports:
  - port: {{ .Values.backstage_port.reverse_port }}
    targetPort: {{ .Values.backstage_port.http }}
  selector:
    app: apache-deploy
